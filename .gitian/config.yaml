annotations:
  render_style: nested
  detect_docs: true
  detect_keywords: true
  tags:
    api:
      kind: api
      color: "#10b981"
      label: "API / RPC"
      urgency: subtle
      pinned: false

groups:
  api:
    color: "#60a5fa"
    label: API
  security:
    color: "#ef4444"
    label: Security

modules:
  components:
    label: "Components"
    color: "#60a5fa"
    dir: src/components
  hooks:
    label: "Hooks"
    color: "#34d399"
    dir: src/hooks
